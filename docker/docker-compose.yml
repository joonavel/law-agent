version: '3.8'

services:
  law-agent:
    build:
      context: ../
      dockerfile: docker/Dockerfile
    container_name: law-agent
    volumes:
      - ../src:/app/src
      - ../data:/app/data
      - ../scripts:/app/scripts
    environment:
      - DISPLAY=:99
      - PYTHONPATH=/app
    networks:
      - law-agent-network
    command: >
      bash -c "
        echo '🚀 Law Agent 시작...' &&
        echo '📊 Chrome 버전 확인:' &&
        google-chrome --version &&
        echo '🔧 ChromeDriver 버전 확인:' &&
        chromedriver --version &&
        echo '🔄 법령 데이터 수집 및 파싱 실행...' &&
        python scripts/collect_and_parse_laws.py
      "

networks:
  law-agent-network:
    driver: bridge